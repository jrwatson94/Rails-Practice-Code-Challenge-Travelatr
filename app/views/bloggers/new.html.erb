<h1>Create a New Profile</h1>

<%= form_for @blogger do |f| %>
    <%=f.label :name %>
    <%=f.text_field :name %>

    <%= f.label :age %>
    <%= f.select :age, options_for_select(1..100) %>

    <%= f.label :bio %>
    <%= f.text_area :bio, :minlength => 30 %>

    <%= f.submit %>
<% end %>